// This is an example reducer to get our app started (so things don't break)
// We will delete once we make actual reducers

export default function (state = [], action) {
  switch(action.type) {
    default:
      return state;
  }
}

